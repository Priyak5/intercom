# Phase 0 runtime. Pinned. Grows one logical dependency at a time, each justified in
# architecture.md §3 (CLAUDE.md rule). DRF, bleach, anthropic, IMAP/SMTP, dnspython,
# email-reply-parser, etc. arrive in their own phases.
Django==5.1.4
channels==4.1.0
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
whitenoise==6.8.2
# Phase 1: DRF (in the architecture stack table) + test tooling.
djangorestframework==3.15.2
pytest==8.3.4
pytest-django==4.9.0
# Phase 4: email channel — IMAP client (imaplib is stdlib but low-level) and quoted-reply
# stripping. SMTP goes through stdlib smtplib directly (no Django mail backend).
IMAPClient==3.0.1
email-reply-parser==0.5.12
# Phase 6: KB article HTML sanitisation (CLAUDE.md I7). Runs bleach.clean() on write
# so templates can render body_html with |safe. FTS5 is SQLite-native (no dep).
bleach==6.2.0
# Phase 7: Claude summarisation via Anthropic SDK. Hard 8s timeout + one retry then
# a deterministic non-LLM fallback keeps the UI never-hanging (CLAUDE.md I8).
anthropic==0.42.0
